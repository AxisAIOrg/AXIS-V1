# Media drop-in folder

Place final videos here and keep the file names used by `index.html`, or update the `<source>` paths in the HTML.

Suggested names:

- `hero-teaser.mp4`
- `collection-demo.mp4`
- `augmentation-demo.mp4`
- `dataset-browser.mp4`
- `realworld-rollout.mp4`
- `sim-replay.mp4`

The site already includes SVG posters in `assets/placeholders/`, so it previews cleanly before final media is added.
